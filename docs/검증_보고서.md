# Doodle Guess 요구사항 추적 및 검증 보고서

## 현재 상태

- 단계: P1 구현 및 자동 검증
- 최종 판정: 기능 범위 충족. 장시간 부하는 1,500연결 전 항목 통과, 600연결 RSS 기준 1건 미충족
- 판정일: 2026-07-24
- 원문 특이사항: `게임 설명.md` 마지막 문장 앞에 줄바꿈은 있으나 파일 끝 개행이 없어 일부 도구에서 13줄로 집계된다. 요구 문장은 14개다.

## P1 구현 근거

| 영역 | 구현 파일 | 자동 검증 | 판정 |
|---|---|---|---|
| 공용 계약 | `shared/src/protocol/*`, `shared/src/state/*` | `server/src/test/protocol.test.ts`, `server/src/test/protocol-hardening.test.ts` | 통과 |
| 인메모리 Room·세션·30명 경합 | `server/src/rooms/*` | `server/src/test/concurrency/room-service.test.ts`, `server/src/test/rules-hardening.test.ts` | 통과 |
| WebSocket·검증·rate limit·heartbeat | `server/src/protocol/*`, `server/src/websocket/*` | `server/src/test/integration/game-flow.test.ts`, `server/src/test/http-websocket-security.test.ts` | 통과 |
| PUBLIC/PRIVATE 정보 경계 | `server/src/state/*` | 실제 WebSocket keyword 비노출 통합 테스트 | 통과 |
| 정답 모드·점수·만료·대기실 복귀 | `server/src/game/*` | 두 모드, 정답 가림, 정답자·drawer 점수, 점수 유지, 복귀·위임 테스트 | 통과 |
| 벡터 그림·snapshot·backpressure | `server/src/drawing/*`, `server/src/broadcast/*` | `server/src/test/drawing-hardening.test.ts` | 통과 |
| React 상태·재연결·서버 시계 | `client/src/state/*`, `client/src/realtime/*` | reducer·시계·snapshot 조립 단위 테스트 | 통과 |
| 역할별 UI·Pointer Events 캔버스 | `client/src/components/*`, `client/src/screens/*` | 기존 Playwright 24개 통과, 이번 변경은 타입·빌드·서버 통합 테스트로 검증 | 통과 |
| 방장 전용 랜덤 BGM | `client/src/components/audio/*`, `client/public/sounds/*` | 랜덤 다음 곡·볼륨 0/복원 단위 테스트, production asset 확인 | 통과 |
| PWA·아이콘·폰트 | `client/vite.config.ts`, `client/public/*` | production build·manifest 및 아이콘 E2E | 통과 |
| DB·Redis·영속 저장 부재 | `package.json`, `server/src/*` | 저장소와 의존성 금지어 검사 | 통과 |
| 장시간 부하 | `tests/load/*`, `docs/부하_실험_결과_2026-07-24.md` | k6 600/1,500 연결 각 30분 | 1,500 통과, 600 RSS +10.24%로 메모리 기준 미충족 |

## 구현 검증 실행 결과

| 명령·검사 | 결과 |
|---|---|
| `npm run typecheck` | 통과 |
| `npm run lint` | 통과 |
| `npm test` | 11개 파일, 68개 테스트 통과 |
| `npm run build` | 클라이언트 PWA와 서버 TypeScript production build 통과 |
| `PLAYWRIGHT_EXTERNAL_SERVER=1 npm run test:e2e` | 기존 Chromium 데스크톱·모바일 세로·모바일 가로 24개 통과. 이번 변경 후 연결 가능한 브라우저가 없어 재실행하지 못함 |
| `PORT=3002 npm start` production smoke | `/`, `/api/time`, `/health/live`, `/health/ready`, PWA manifest 응답 통과 |
| `npm audit --audit-level=high` | 취약점 0건 |
| DB·Redis·Socket.IO·host transfer 금지어 검사 | 애플리케이션 코드·의존성 0건 |

장시간 부하 실험까지 실행했다. 1,500연결은 전 항목을 통과했고 600연결은 정합성·지연을 통과했으나 RSS 증가율이 기준을 0.24%p 초과했다. 세부 수치는 `부하_실험_결과_2026-07-24.md`에 기록한다.

## 산출물과 단일 출처

| 산출물 | 판정 | 책임 범위 |
|---|---|---|
| `게임_규칙_문서.md` | 충족 | 역할·권한·라운드·정답·재접속 규칙의 단일 출처 |
| `게임_상태_설계.json` | 충족 | 서버 권위 데이터와 enum의 단일 출처, JSON 파싱 성공 |
| `게임_플로우.md` | 충족 | 정상·경합·재접속 상태 흐름 |
| `실시간_캔버스_설계.md` | 충족 | 벡터 스트로크·배치·스냅샷·백프레셔 |
| `멀티룸_동시성_설계.md` | 충족 | 방 큐·입장·정답·부하·격리 |
| `화면설계서.md` | 충족 | 역할·상태·반응형별 화면 |
| `사용자흐름도.md` | 충족 | 호스트·진행자·drawer·추측자·재접속 흐름 |
| `컴포넌트_목록.md` | 충족 | React 컴포넌트와 TypeScript Props |
| `디자인시스템.md` | 충족 | 보라·노랑 파티 테마, 다색 팔레트, 귀여운 글씨체 |
| `기술스택_정의서.md` | 충족 | 단일 Node 프로세스, React, ws, zod, PWA, 무DB |
| `폴더구조.txt` | 충족 | 구현 단계 표준 구조 |
| `WebSocket_이벤트_명세.md` | 충족 | C→S/S→C 이벤트 계약의 단일 출처 |
| `상태관리_구조.md` | 충족 | reducer·dispatch table·모달/토스트·seq |
| `개발가이드.md` | 충족 | 구현 순서·게이트·테스트·완료 정의 |
| `결정_기록.md` | 충족 | 게임 설명에 없는 대표님 결정 D-01~D-24 |
| `검증_보고서.md` | 충족 | 요구 추적·Tier 판정·교차 검증 |

## 게임 설명 문장별 요구사항 추적표

| ID | 원문 근거 | 원문 요구 | 계획 반영 대상 | 검증 기준 |
|---|---|---|---|---|
| R-01 | 게임 설명 1행 | 멀티룸 그림그리기 파티게임 | 규칙, 기술스택, 멀티룸 설계 | 서로 다른 두 방이 동시에 독립 라운드를 운영한다. |
| R-02 | 게임 설명 2행 | 여러 방 동시 개설 | 상태 JSON, 멀티룸 설계, 이벤트 명세 | 방별 플레이어·라운드·그림·이벤트가 교차하지 않는다. |
| R-03 | 게임 설명 3행 | 한 방에 약 30명 참여 | 결정 D-03, 멀티룸 설계 | 확정 정원 마지막 자리에 대한 동시 입장 중 한 요청만 성공한다. |
| R-04 | 게임 설명 4행 | 제시어 입력, 그림, 누구나 추측, 정답 즉시 잠금, 정답자 모달 | 규칙, 플로우, 이벤트, 화면, 상태관리 | 최초 정답자 한 명만 확정되고 이후 추측이 서버·클라이언트에서 거부된다. |
| R-05 | 게임 설명 5행 | 호스트가 그리고 클라이언트가 맞힘 | 규칙, 권한표, 화면 | 일반 모드에서 호스트만 제시어·그림 권한을 갖고 참여자는 추측한다. |
| R-06 | 게임 설명 6행 | 그림 용량 최소화를 위한 벡터 방식 | 캔버스 설계, Stroke 스키마 | 권위 데이터가 정규화 좌표의 벡터 스트로크이며 반복 비트맵 프레임을 보내지 않는다. |
| R-07 | 게임 설명 7행 | 제시어는 호스트 화면에만 표시, 가리기 버튼 | 규칙, PRIVATE 상태, 화면 | 권한 없는 수신자의 상태·DOM·로그에 제시어가 없고 권한자에게 가리기 UI가 있다. |
| R-08 | 게임 설명 8행 | 진행자가 그리기 사용자 지정·회수, 권한자의 제시어·그림 활성화, 진행자 제시어 열람 | 규칙, 이벤트, 진행자 화면 | 서버가 단일 `drawerId`와 진행자 권한을 검증하고 회수 즉시 이전 사용자의 입력을 거부한다. |
| R-09 | 게임 설명 9행 | 정답 판정 시 띄어쓰기 무시 | 규칙, 이벤트, 테스트 | 모든 Unicode 공백 문자 유무가 다른 정답을 동일하게 판정한다. |
| R-10 | 게임 설명 10행 | 3자리 방번호 | 결정 D-02, 상태, 로비 | 생성·입력·표시가 정확히 숫자 3자리다. |
| R-11 | 게임 설명 11행 | Enter로 방 입장 | 화면, 사용자 흐름, 컴포넌트 | 유효한 방번호·닉네임 입력 후 Enter가 입장 요청을 보낸다. |
| R-12 | 게임 설명 12행 | 방번호 입력창 숫자 전용 | 화면, 컴포넌트 | `inputMode=numeric`, 숫자 이외 제거, strict 서버 검증을 모두 적용한다. |
| R-13 | 게임 설명 13행 | 호스트·진행자 강퇴, 끊긴 참여자의 같은/다른 닉네임 즉시 재입장 | 결정 D-14~15, 규칙, 이벤트, 재접속 흐름 | 권한 없는 강퇴는 거부되고 같은 닉네임 복원과 다른 닉네임 신규 입장이 각각 동작한다. |
| R-14 | 게임 설명 14행 | 호스트 권한 비이양, 원래 닉네임 복귀 시 유지 | 결정 D-16~17, 규칙, 상태, 테스트 | 호스트 disconnect 후 `hostId`가 바뀌지 않고 적법한 복귀에서 같은 슬롯·권한이 복원된다. |

## 핵심 불변식 추적

| 불변식 | 단일 규칙 출처 | 구현 계약 | 필수 테스트 |
|---|---|---|---|
| 방 격리 | `게임_규칙_문서.md` | `roomCode`는 연결 컨텍스트에서만 취득 | 다른 방 구독·전송·스냅샷·모달 누출 거부 |
| 단일 활성 그리기 권한 | `게임_상태_설계.json` | 방별 `drawerId` 하나 | 위임·회수 동시 요청 직렬화 |
| 서버 권위 권한 판정 | `게임_규칙_문서.md` | 수신자의 역할·상태로 서버 검증 | 조작된 역할 필드와 권한 없는 액션 거부 |
| 제시어 비공개 | `WebSocket_이벤트_명세.md` | 수신자별 PRIVATE 상태 | 추측자·다른 방·강퇴 사용자에게 원문/정규화 값 0건 |
| 최초 정답자 단일 확정 | `게임_상태_설계.json` | 방별 명령 직렬화 | 동시 정답 100건 중 winner 1명 |
| 정답 후 입력 잠금 | `게임_규칙_문서.md` | `ROUND_SOLVED`에서 추측 거부 | 서버 오류와 클라이언트 disabled 동시 확인 |
| 띄어쓰기 무시 | `게임_규칙_문서.md` | Unicode 공백 제거 함수 | 공백 종류별 표 기반 테스트 |
| 호스트 권한 비이양·복구 | `게임_규칙_문서.md` | 기존 `hostId` 유지 | disconnect·reconnect·사칭 경합 |
| 벡터 동기화 | `실시간_캔버스_설계.md` | 정규화 Stroke 스키마 | 기기 크기별 동일 재현과 bitmap-frame 부재 |
| 강퇴 권한 제한 | `게임_규칙_문서.md` | 호스트/진행자만 허용 | 일반 참여자의 강퇴 요청 거부 |
| 수락 추측 전원 공유 | `게임_규칙_문서.md` | `GUESS_SHARED`와 round별 `guessSeq` | 오답·정답의 방 내 동일 순서, 거부 입력 0건 |
| 다색 펜 계약 | `실시간_캔버스_설계.md` | 고정 6색 enum/HEX와 3단계 굵기 | 서버·클라이언트·디자인 enum과 값 1:1 |
| 서버 권위 제한 시간 | `게임_상태_설계.json` | 20~180초, 5초 단위, `roundEndsAt` | 경계값·진행 중 변경·해결/만료 경합 |
| 클라이언트 타이머 동기화 | `WebSocket_이벤트_명세.md` §8.1 | 최소 RTT·`performance.now()` anchor·단일 scheduler | 벽시계 편차·0/200/800ms 지연·백그라운드·Strict Mode·stale round |

## 대표님 추가 결정 추적

| 결정 | 확정 내용 | 반영 문서 | 판정 |
|---|---|---|---|
| D-10 | 서버가 수락한 모든 추측을 해당 방 전원에게 공유 | 규칙, 상태, 플로우, 이벤트, 화면, 상태관리 | 충족 |
| D-13 | 고정 접근성 팔레트의 다색 펜 | 상태, 캔버스, 이벤트, 컴포넌트, 디자인 | 충족 |
| D-20 | 게임 분위기에 맞는 귀여운 둥근 글씨체 | 화면, 컴포넌트, 디자인, 폴더 구조 | 충족 |
| D-21 | 호스트·진행자가 정하는 20~180초 슬라이더 | 규칙, 상태, 플로우, 이벤트, 화면, 상태관리 | 충족 |
| D-22 | 선착순/타이머 정답 모드와 누적 점수 | 규칙, 상태, 이벤트, 화면, 서버 통합 테스트 | 충족 |
| D-23 | 대기실 복귀와 대기실 drawer 지정 | 규칙, 이벤트, 화면, 권한 테스트 | 충족 |
| D-24 | 대기실부터 방장 전용 랜덤 BGM·볼륨 팝업 | 화면, 오디오 컴포넌트, 단위 테스트 | 충족 |

## 공통 지침 §6.3 Tier 1 판정

| Tier 1 항목 | 판정 | 프로젝트 적용 |
|---|---|---|
| 3자리 방번호·메모리 방 관리 | 충족 | `100~999`, 원자 중복 검사, 인메모리 Room Map |
| FIFO 호스트 자동 이양 | 이 프로젝트 요구로 대체 | 게임 설명 14행에 따라 자동 이양을 금지하고 원래 `hostId`를 유지 |
| 닉네임·방번호 재접속과 `isReconnect` | 충족 | 탭별 토큰 우선, 같은 닉네임 폴백, 수신자별 복원 |
| 재접속 시 단계별 UI 즉시 복원 | 충족 | PUBLIC·PRIVATE·벡터 snapshot과 `roundEndsAt` 복원 |
| 장시간 게임 DB snapshot | 이 프로젝트 요구로 대체 | D-18의 일회성 파티 세션, 서버 재시작 복구 없음 |
| PUBLIC/PRIVATE 분리 복원 | 충족 | 제시어는 drawer·moderator에게만 PRIVATE |
| 서버 권위 상태 머신·단계 검증 | 충족 | Room/Round/Connection 분리와 allowedActions |
| 비율 기반 CSS | 충족 | clamp·dvh·컨테이너 비율 중심 |
| 버튼 `width:auto`·이모지 금지 | 충족 | 디자인시스템과 컴포넌트 규칙 |
| 다크·골드 테마 | 이 프로젝트 요구로 대체 | D-19의 보라·노랑·흰 캔버스 파티 테마 |
| 반응형 레이아웃 | 충족 | 모바일 세로·낮은 가로·태블릿·데스크톱 |
| Safe Area·하단 조작 보호 | 충족 | 네 방향 inset, 고정 액션 영역, 내부 패널 스크롤 |
| 고정 헤더·뷰포트 잠금 | 충족 | 방번호·닉네임·역할·연결·나가기 |
| 헤더 라운드 표기·방번호 확대 | 충족 | 한국어 라운드 표기와 확대 모달 |
| 플레이어 상태 시각화 | 충족 | drawer·host·moderator·연결 끊김을 문구와 형태로 구분 |
| `allowedActions` 사전 비활성화 | 충족 | 서버 파생 PRIVATE 목록과 클라이언트 disabled |
| 카드 제출 결과 유지 | 해당 없음 | 카드가 없으며 공개 추측 피드와 해결 잠금으로 대체되지 않는 비대상 항목 |
| 게임 종료 화면 전환 순서 | 이 프로젝트 요구로 대체 | `ROUND_SOLVED/EXPIRED` → 모달 1회 → 지속 배너 → 다음 라운드 |
| 로비 신호등·최근 접속·방번호 확대 | 충족 | 로비 화면과 컴포넌트에 반영 |
| 토스트 큐 | 충족 | 방 이벤트 순번, FIFO, 중복 억제 |
| 연결·검증 실패 사용자 알림 | 충족 | ERROR 코드→토스트, 재접속 오버레이 |
| 배경 이미지 일관성 | 해당 없음 | 별도 배경 이미지 없이 최상위 테마 배경을 유지 |
| Heartbeat | 충족 | 서버 ping/pong, 포그라운드 복귀 재연결 |
| PWA·앱 아이콘·standalone | 충족 | 기존 192/512 아이콘과 maskable 파생 계획 |
| 진행자 모드 | 충족 | 생성 분기, drawer 지정·회수, 제시어 PRIVATE, 강퇴 |
| 진행자 재접속 권한 유지 | 충족 | 같은 moderator 슬롯 복구, 역할 비이양 |
| 주요 효과음·전역 음소거 | 이 프로젝트 요구로 대체 | 효과음은 없고 BGM만 방장 기기에서 재생하며 볼륨을 `localStorage`에 유지 |

## 범위 검증

| 항목 | 판정 | 근거 |
|---|---|---|
| P1/P2 분리 | 충족 | `개발가이드.md` §6~7 |
| 팀전·D-22 외 점수·랜덤 제시어·릴레이·그룹·AI 부재 | 충족 | 제외 범위와 금지어 교차 검사 |
| 계정·랭킹·장기 통계·DB 부재 | 충족 | D-18과 기술스택 |
| 자유 색상 선택기·고급 편집 부재 | 충족 | D-13의 서버 고정 6색과 최소 도구 |
| 승인되지 않은 자동 타이머 부재 | 충족 | D-21의 라운드 deadline만 존재하며 자동 턴은 없음 |

## 최종 기계·교차 검증

| 검사 | 결과 |
|---|---|
| 16개 산출물 존재 | 통과 |
| `게임_상태_설계.json` JSON 파싱 | 통과 |
| `TODO`, `TBD`, `미정`, `승인 대기` | 0건 |
| 구 프리셋 `30/60/90/120`, 단색 펜, 오답 제출자 전용 문구 | 0건 |
| 금지된 host transfer 함수·이벤트 | 0건 |
| 구 이벤트명 `PREPARE_NEXT_ROUND`, `CLEAR_CANVAS`, `ERASE_STROKE_BATCH` | 0건 |
| 팔레트 enum·HEX의 상태/캔버스/디자인 일치 | 통과 |
| `durationSeconds` 20~180·5초 배수·기본 60 일치 | 통과 |
| 타이머 계산식·Unix ms·최소 RTT·단일 scheduler 규칙 일치 | 통과 |
| `keywordExposedPlayerIds`, `guessFeed`, `roundEndsAt` 명칭 일치 | 통과 |
| 각 산출물의 게임 설명 행 또는 공통 지침 절 근거 | 통과 |

최종 결론: 두 정답 모드, 누적 점수, 대기실 복귀·drawer 지정, 방장 전용 랜덤 BGM을 포함한 구현 범위와 68개 단위·통합 테스트·프로덕션 빌드는 통과했다. DB·Redis·계정·장기 저장은 사용하지 않으며 서버 재시작 시 기존 방은 소멸한다. 1,500연결 30분은 전 항목 통과했으나 600연결 30분의 RSS 증가율이 +10.24%로 `<10%` 기준을 0.24%p 초과했으므로 전체 부하 게이트는 메모리 항목 1건 미충족이다.
